This is a monorepo
mobile app => apps/mobile (for front end client)
relay server => =>apps/relay-server (Relay server which acts as middleman between client and the agent server)
Server => apps/server (Server which runs on the user's laptop)
web => apps/web (Web interface for front end client)

right now ignore the mobile.